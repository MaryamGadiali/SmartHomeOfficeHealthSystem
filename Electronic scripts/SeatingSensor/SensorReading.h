#ifndef SENSORREADINGH
#define SENSORREADINGH

// this should record the ultrasonic reading, if user is not within 50cm, then they are not present

class SensorReading{
    public:

    static bool takeDistanceReading();
};

#endif
